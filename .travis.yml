language: java

dist: trusty

jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
  - oraclejdk8

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - java -version
  - echo $JAVA_HOME
  - mvn test -Dft

sudo: false
group: deprecated
