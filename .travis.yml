language: java

jdk:
  - oraclejdk8
  - openjdk7
  - oraclejdk7

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - mvn test -Dft

sudo: true
group: deprecated
dist: trusty
